import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.obrysoft.nika',
  appName: 'Nika-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
